// Import commands.js using ES2015 syntax:
import "./commands";

// Alternatively you can use CommonJS syntax:
// require('./commands');

// If using TypeScript, configure Cypress types here:
declare global {
  namespace Cypress {
    interface Chainable {
      // Define custom command types if needed
    }
  }
}
